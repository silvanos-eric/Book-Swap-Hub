import React from 'react';
import { Link } from 'react-router-dom';

const BookCard = ({ book }) => {
  return (
    <div className="book-card">
      {/* a book card fro each book */}
    </div>
  );
};

export default BookCard;
